
void busquedabinaria ( int b, struct list* L){

  struct list* x =L;
  int p=0; posicion;
  
  while ( x != NULL) {
    if (b==x) {
      posicion = p; 
      return posicion ; 
  }
  p++;
  x=x->siguiente; 

}
return -1;
}

